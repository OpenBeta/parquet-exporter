# OpenBeta Parquet Exporter Configuration

export:
  api_url: "https://api.openbeta.io/graphql"

  # Filter by country (leave empty for worldwide)
  regions: []
    # - USA
    # - Canada

  output:
    filename: "openbeta-climbs.parquet"
    compression: "snappy"  # Options: snappy, gzip, zstd
